# Drop API documentation here

Put vendor/manual/API documents here (`.md`, `.txt`, `.rst`, `.json`, `.html`, `.pdf`, `.c`, `.h`, `.cpp`, `.hpp`).
Then run:

```bash
python3 -m uvscanx rag index
```

The generated lexical RAG index is written to `data/rag/index/`.
